出处：Tor Lattimore, *A Diffusion Analysis of Policy Gradient for Stochastic Bandits*, arXiv:2603.10219, Algorithm 2 and Lemma 1 (conservation), PDF pp. 2-3；Zotero item `HU7I7KHR`，附件 `DLC6PFM7`。
标签：#policy-gradient #stochastic-bandit #softmax-parameterization #SDE #conservation-law

> [!question] (2026-06-19) 第1次做，正确总0次，上次新题
> **题目：补全文中 Lemma 1 的 conservation 证明。**
>
> 考虑 $k$ 臂 stochastic bandit。奖励均值为 $\mu\in\mathbb R^k$，连续时间观测过程满足
> $$
> dX_t=\operatorname{diag}(\pi_t)\mu\,dt+\operatorname{diag}(\sqrt{\pi_t})\Sigma^{1/2}dB_t.
> $$
> 策略由 softmax 参数化：
> $$
> \pi_t=\pi(\theta_t),\qquad
> \pi_{t,a}=\frac{\exp(\theta_{t,a})}{\sum_{b=1}^k\exp(\theta_{t,b})}.
> $$
> Algorithm 2 的连续时间 policy gradient 更新为
> $$
> d\theta_t=\eta(I_d-\pi_t\mathbf 1^\top)dX_t,
> \qquad
> \theta_0=0\in\mathbb R^k.
> $$
> 证明 Lemma 1:
> $$
> \sum_{a=1}^k\theta_{t,a}=0
> \qquad\text{almost surely for all }t.
> $$
> 特别说明：为什么文中一句
> $$
> \langle \mathbf 1,d\theta_t\rangle=0
> $$
> 就能推出这个结论？

> [!success]- 核心思路（点击展开）
> 一句话结论：**$\langle\mathbf 1,d\theta_t\rangle=0$ 的意思就是所有坐标的瞬时增量之和为 $0$，所以 $\sum_a\theta_{t,a}$ 是守恒量；初始化这个和是 $0$，因此任意时刻仍然是 $0$。**
>
> 先把要守恒的量单独记出来：
> $$
> S_t:=\langle\mathbf 1,\theta_t\rangle
> =
> \sum_{a=1}^k\theta_{t,a}.
> $$
> 因为 $S_t$ 是 $\theta_t$ 的线性函数，所以
> $$
> dS_t
> =
> d\langle\mathbf 1,\theta_t\rangle
> =
> \langle\mathbf 1,d\theta_t\rangle
> =
> \sum_{a=1}^k d\theta_{t,a}.
> $$
> 这里没有 Itô correction，因为线性函数的二阶导数为 $0$。
>
> 代入 Algorithm 2 的更新式：
> $$
> \begin{aligned}
> \langle\mathbf 1,d\theta_t\rangle
> &=
> \eta\,\mathbf 1^\top(I_d-\pi_t\mathbf 1^\top)dX_t\\
> &=
> \eta\left(\mathbf 1^\top-\mathbf 1^\top\pi_t\mathbf 1^\top\right)dX_t.
> \end{aligned}
> $$
> 由于 $\pi_t$ 是概率分布，
> $$
> \mathbf 1^\top\pi_t
> =
> \sum_{a=1}^k\pi_{t,a}
> =
> 1.
> $$
> 因此
> $$
> \mathbf 1^\top-\mathbf 1^\top\pi_t\mathbf 1^\top
> =
> \mathbf 1^\top-1\cdot\mathbf 1^\top
> =
> 0^\top.
> $$
> 所以
> $$
> dS_t=\langle\mathbf 1,d\theta_t\rangle=0.
> $$
>
> 更严格地说，这不是“期望为 $0$”，而是乘在 $dX_t$ 前面的行向量已经恒等于 $0^\top$；因此无论 $dX_t$ 的 drift 项还是 martingale noise 项是什么，投到 $\mathbf 1$ 方向后都完全消失。于是
> $$
> S_t=S_0.
> $$
> 又因为 $\theta_0=0$，
> $$
> S_0=\sum_{a=1}^k\theta_{0,a}=0.
> $$
> 于是
> $$
> \sum_{a=1}^k\theta_{t,a}=S_t=S_0=0
> $$
> almost surely。
>
> **直观解释。**
> softmax 有平移冗余：
> $$
> \pi(\theta)=\pi(\theta+c\mathbf 1).
> $$
> 所有 $\theta_a$ 同时加同一个常数，策略完全不变。所以 $\mathbf 1$ 方向是一个无效方向。Algorithm 2 里的矩阵
> $$
> I_d-\pi_t\mathbf 1^\top
> $$
> 会把更新增量压到
> $$
> \{\theta:\langle\mathbf 1,\theta\rangle=0\}
> $$
> 这个 $(k-1)$ 维超平面里。PG 更新只改变 action 之间的相对偏好，不改变所有坐标的共同平移量。
>
> **后面为什么有用。**
> Lemma 2 前的直觉用到 Lemma 1：如果
> $$
> \sum_{a=1}^k\theta_{t,a}=0,
> $$
> 那么至少存在一个 $b$ 使得 $\theta_{t,b}\ge 0$；否则如果所有坐标都小于 $0$，总和也会小于 $0$。因此
> $$
> \max_b\theta_{t,b}\ge 0.
> $$
> 对任意 action $a$，
> $$
> \pi_{t,a}
> =
> \frac{\exp(\theta_{t,a})}{\sum_b\exp(\theta_{t,b})}
> \le
> \exp(\theta_{t,a}),
> $$
> 因为分母至少包含某个 $\exp(\theta_{t,b})\ge 1$ 的项。于是如果某个 $\theta_{t,a}$ 特别负，则 $\pi_{t,a}$ 很小，policy gradient 对这个坐标的后续更新也会变弱。这就是 Lemma 2 直觉的来源。
>
> **最短记忆版。**
> $$
> \langle\mathbf 1,d\theta_t\rangle=0
> $$
> 就是“所有坐标增量之和为 $0$”。所以
> $$
> d\left(\sum_a\theta_{t,a}\right)=0.
> $$
> 初始化时 $\sum_a\theta_{0,a}=0$，因此整个轨迹始终留在 $\sum_a\theta_{t,a}=0$ 的规范代表超平面里。

> [!info]- 错误原因（点击展开）
> 这题容易卡住，不是因为 SDE 很深，而是因为文中把三个小步骤压成了一句话。
>
> 第一，没把
> $$
> \langle\mathbf 1,d\theta_t\rangle
> $$
> 翻译成
> $$
> d\left(\sum_a\theta_{t,a}\right).
> $$
> 它说的是线性统计量 $S_t=\sum_a\theta_{t,a}$ 的微分，而不只是某个形式上的内积。
>
> 第二，容易把 $\langle\mathbf 1,d\theta_t\rangle=0$ 误读成“均值方向上的期望为 $0$”。这里更强：矩阵计算直接给出
> $$
> \mathbf 1^\top(I_d-\pi_t\mathbf 1^\top)=0^\top,
> $$
> 所以 $\mathbf 1$ 方向的 drift 和 noise 都没有。
>
> 第三，忘记 softmax 的共同平移冗余。Lemma 1 的意义是选定一个规范代表：虽然 $\theta$ 和 $\theta+c\mathbf 1$ 表示同一个策略，Algorithm 2 从 $\theta_0=0$ 出发后始终待在 $\langle\mathbf 1,\theta\rangle=0$ 这个超平面中。
>
> 下次检查点：看到 conservation lemma 时，先找守恒量 $S_t$，再算 $dS_t$。如果 $dS_t=0$ 且 $S_0=0$，结论就是 $S_t=0$，不要把它复杂化成新的 SDE 问题。
