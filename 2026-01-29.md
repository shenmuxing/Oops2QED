

> [!question] (2026-01-29) 第1次做，正确总0次，上次错误
> **背景与定义：**
> 1. **策略定义**：Softmax 策略 $\pi_{\theta}(a|s) \propto \exp(\theta_{s,a})$，参数 $\theta \in \mathbb{R}^{|\mathcal{S}||\mathcal{A}|}$。
> 2. **向量定义**：设 $w$ 为参数空间中的任意向量，维度与 $\theta$ 相同，其分量记为 $w_{s', a'}$。
> 3. **已知梯度 (Eq 0.1)**：
>    $$\frac {\partial \log \pi_ {\theta} (a | s)}{\partial \theta_ {s ^ {\prime} , a ^ {\prime}}} = \mathbb {1} [ s = s ^ {\prime} ] \left(\mathbb {1} [ a = a ^ {\prime} ] - \pi_ {\theta} (a ^ {\prime} | s)\right)$$
> 
> **练习目标：**
> 请根据以上已知信息，推导出 $w^{\top} \nabla_{\theta} \log \pi_{\theta} (a | s)$ 的最终简化形式。

> [!success]- 核心思路（点击展开）
> **核心思路：**
> 这里本质上是计算向量点积（Dot Product）。关键在于利用梯度中的指示函数 $\mathbb{1}[s=s']$ 消除掉所有非当前状态 $s$ 的项，然后利用 $\mathbb{1}[a=a']$ 筛选出目标动作项。
>
> **完整推导步骤：**
>
> 4. **展开点积定义**：
>    点积等于各分量相乘之和。我们需要对所有可能的状态 $s'$ 和动作 $a'$ 进行求和：
>    $$w^{\top} \nabla_{\theta} \log \pi_{\theta} (a | s) = \sum_{s' \in \mathcal{S}} \sum_{a' \in \mathcal{A}} w_{s', a'} \frac{\partial \log \pi_{\theta}(a|s)}{\partial \theta_{s', a'}}$$
>
> 5. **代入已知梯度公式**：
>    $$= \sum_{s'} \sum_{a'} w_{s', a'} \cdot \underbrace{\mathbb{1}[s = s']}_{\text{只在 } s'=s \text{ 时非0}} \cdot (\mathbb{1}[a = a'] - \pi_{\theta}(a'|s))$$
>
> 6. **消除状态求和 $\sum_{s'}$**：
>    由于指示函数 $\mathbb{1}[s=s']$ 的存在，求和中只有 $s'=s$ 的项保留下来，其余为 0：
>    $$= \sum_{a' \in \mathcal{A}} w_{s, a'} (\mathbb{1}[a = a'] - \pi_{\theta}(a'|s))$$
>
> 7. **拆分动作求和并化简**：
>    $$= \underbrace{\sum_{a'} w_{s, a'} \mathbb{1}[a = a']}_{\text{第一部分}} - \underbrace{\sum_{a'} w_{s, a'} \pi_{\theta}(a'|s)}_{\text{第二部分}}$$
>
>    * **第一部分**：由于 $\mathbb{1}[a = a']$，只有当遍历到 $a'=a$ 时有值，故结果为 $w_{s, a}$。
>    * **第二部分**：这是 $w$ 在状态 $s$ 下关于策略分布的期望（即文中定义的 $\overline{w}_s$）。
>
> **最终结果：**
> $$w^{\top} \nabla_{\theta} \log \pi_{\theta} (a | s) = w_{s, a} - \sum_{a' \in \mathcal{A}} w_{s, a'} \pi_{\theta}(a'|s)$$

> [!info]- 错误原因（点击展开）
> 不会


---

> [!question] (记录日期：2026-03-15) 第1次做，正确总0次，上次错误
> 求下式的解析解：
> $$
> \widehat{\mathbf{w}}_{k, h} \leftarrow \underset{\mathbf{w} \in \mathbb{R}^{d}}{\operatorname{argmin}} \;
> \lambda\|\mathbf{w}\|_{2}^{2}
> +
> \sum_{i=1}^{k-1} \bar{\sigma}_{i, h}^{-2}
> \left(
> \mathbf{w}^{\top} \boldsymbol{\phi}(s_{h}^{i}, a_{h}^{i})
> -
> V_{k, h+1}(s_{h+1}^{i})
> \right)^{2}
> $$
> 其解析解为：
> $$
> \widehat{\mathbf w}_{k,h}
> =
> \left(
> \lambda I
> +
> \sum_{i=1}^{k-1}\bar{\sigma}_{i,h}^{-2}
> \boldsymbol{\phi}(s_h^i,a_h^i)\boldsymbol{\phi}(s_h^i,a_h^i)^\top
> \right)^{-1}
> \left(
> \sum_{i=1}^{k-1}\bar{\sigma}_{i,h}^{-2}
> V_{k,h+1}(s_{h+1}^i)\,\boldsymbol{\phi}(s_h^i,a_h^i)
> \right).
> $$

> [!success]- 核心思路（点击展开）
> 这是一个带权 Ridge Regression / 加权最小二乘问题。
>
> 先记
> $$
> x_i := \boldsymbol{\phi}(s_h^i,a_h^i), \qquad
> y_i := V_{k,h+1}(s_{h+1}^i), \qquad
> \omega_i := \bar{\sigma}_{i,h}^{-2}.
> $$
>
> 原问题可写为
> $$
> \widehat{\mathbf w}_{k,h}
> =
> \arg\min_{\mathbf w\in\mathbb R^d}
> \left[
> \lambda \|\mathbf w\|_2^2
> +
> \sum_{i=1}^{k-1}\omega_i(\mathbf w^\top x_i-y_i)^2
> \right].
> $$
>
> 对目标函数关于 $\mathbf w$ 求梯度并令其为零：
> $$
> 2\lambda \mathbf w
> +
> 2\sum_{i=1}^{k-1}\omega_i(\mathbf w^\top x_i-y_i)x_i
> = 0.
> $$
>
> 整理得
> $$
> \left(
> \lambda I + \sum_{i=1}^{k-1}\omega_i x_i x_i^\top
> \right)\mathbf w
> =
> \sum_{i=1}^{k-1}\omega_i y_i x_i.
> $$
>
> 因而解析解为
> $$
> \widehat{\mathbf w}_{k,h}
> =
> \left(
> \lambda I
> +
> \sum_{i=1}^{k-1}\bar{\sigma}_{i,h}^{-2}
> \boldsymbol{\phi}(s_h^i,a_h^i)\boldsymbol{\phi}(s_h^i,a_h^i)^\top
> \right)^{-1}
> \left(
> \sum_{i=1}^{k-1}\bar{\sigma}_{i,h}^{-2}
> V_{k,h+1}(s_{h+1}^i)\,\boldsymbol{\phi}(s_h^i,a_h^i)
> \right).
> $$

> [!info]- 错误原因（点击展开）
> 容易卡住的点通常不是计算本身，而是没有把它识别成一个标准的加权 Ridge Regression。
>
> 具体来说：
> 1. 没有把目标函数整理成“二次项 + 加权残差平方和”的标准形式；
> 2. 对向量变量求导不熟，尤其是
>    $$
>    \nabla_{\mathbf w}(\mathbf w^\top x - y)^2 = 2(\mathbf w^\top x - y)x;
>    $$
> 3. 不熟悉“梯度设为零 \(\Rightarrow\) 正规方程 \(\Rightarrow\) 解析解”这一套流程；
> 4. 对
>    $$
>    \sum_i \omega_i x_i x_i^\top
>    $$
>    这种矩阵形式不敏感，导致整理不出来最终的逆矩阵表达式。

