# AGENTS 协作协议（错题本）

本仓库是 Obsidian 错题本，目标是记录并复习「公式推导卡壳点 / 易错点 / 不熟点」，通过题卡化训练提升学术推导能力。

## 0. 文档维护硬约束
- 优先手动修改内容，不做脚本批量替换；仅在替换风险足够低时才使用脚本。
- 对将要修改的 Markdown 文件，修改前后都执行 UTF-8 检测并记录结果，避免编码混乱。

## 1. 基本定位
- 你负责给出练习需求（题目来源、知识点、难度、语言偏好、是否要提示）。
- Codex 负责产出高质量题卡，并帮助你复盘错误原因与复习节奏。
- 默认兼容 `GEMINI.md` 的题卡格式，避免历史内容断层。

## 2. 文件与命名规则
- 每次新建题卡文件：`codex_YYYY-MM-DD.md`
- 一个文件可包含多道题，按时间顺序追加。
- 历史文件不改写原文；若需修订，新增“修订版”题卡并标注日期。

## 3. 题卡模板（默认）

> [!question] (YYYY-MM-DD) 第n次做，正确总x次，上次正确/错误/新题  
> **题目：**（完整题干，含符号定义与必要前提）
>
> [!success]- 核心思路（点击展开）  
> 给出可复现的推导链条：关键恒等式 -> 中间步骤 -> 最终结论。  
> 如有多解法，先主线再补充替代思路。
>
> [!info]- 错误原因（点击展开）  
> 必须定位到“具体缺口”：知识点缺失 / 步骤跳跃 / 符号误用 / 计算粗心。

建议附加（可选）：
- `出处：` 书名 + 章节 + 题号
- `标签：` 如 `#policy-gradient #measure-theory #bandit`

## 4. 交互协议（你可以直接这样下指令）
- `出题：`  
  例：`出一题 Softmax policy gradient，难度中等，不给提示。`
- `讲解：`  
  例：`把这题的核心推导改成“先直觉后严格证明”。`
- `复盘：`  
  例：`根据我这次错误，重写“错误原因”，要具体到哪一步断了。`
- `变式：`  
  例：`基于这题再出2个变式：一个降难度，一个升难度。`
- `复习：`  
  例：`从最近7天题卡里挑3题做间隔复习。`

## 5. Codex 的执行标准
- 题目必须自洽：符号、条件、目标齐全，避免“只给结论让人猜”。
- 解答必须可检查：关键等式不给“魔法跳步”。
- 错因必须可行动：读完后知道下次如何避免同类错误。
- 中文优先；你指定英文时可切换双语或全英文。

## 6. 统计字段更新约定
- `第n次做`：同一题每重做一次 +1。
- `正确总x次`：累计答对次数。
- `上次正确/错误/新题`：反映上一次作答结果。
- 若你未提供作答结果，Codex 默认写 `上次新题` 或保持不变并提示待确认。

## 7. 默认工作流
1. Codex 根据你的指令生成题卡草稿。  
2. 你作答后给结果（正确/错误 + 卡住位置）。  
3. Codex 更新统计字段 + 重写“错误原因” + 给下一轮复习建议。

## 8. 向后兼容声明
- `GEMINI.md` 仍可作为历史协议参考。
- 从现在起默认以 `AGENTS.md` 为主协议执行。
