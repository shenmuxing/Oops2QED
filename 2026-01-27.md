
> [!question] (2026-01-27) 第2次做，正确总1次，上次正确
> 令梯度为0求解权重 $\mathbf{w}$。
> $$ \nabla_{\mathbf{w}} J(\mathbf{w})=-2 \sum_{\tau=1}^{k-1}\left(y_{\tau}-\mathbf{w}^{\top} \phi_{\tau}\right) \phi_{\tau}+2 \lambda \mathbf{w} $$ 
> **符号定义**：
> - $\mathbf{w}$：权重向量
> - $\phi_{\tau}$：特征向量
> - $y_{\tau}$：标量（观测值/标签）
> - $\lambda$：标量（正则化系数）
> 
> 请写出令梯度为零后的中间方程，以及最终 $\mathbf{w}$ 的表达式。

> [!success]- 核心思路（点击展开）
>  通过对损失函数求导并令梯度为零，整理即可得到。
> 
>  **完整推导步骤：**
>  令梯度为零得到正规方程：
>  $$ 
>  \left(\sum_{\tau=1}^{k-1} \phi_{\tau} \phi_{\tau}^{\top}+\lambda I\right) \mathbf{w}=\sum_{\tau=1}^{k-1} \phi_{\tau} y_{\tau} 
>  $$ 
> 
>  两边同时左乘逆矩阵，因此得到：
>  $$ 
>  \mathbf{w}_{h}=\left(\sum_{\tau=1}^{k-1} \phi_{\tau} \phi_{\tau}^{\top}+\lambda I\right)^{-1}\left(\sum_{\tau=1}^{k-1} \phi_{\tau} y_{\tau}\right) 
>  $$ 

> [!info]- 错误原因（点击展开）
> $\mathbf{w}^T\phi_{\tau}$ 为标量

---

> [!question] (2026-01-29) 第4次做，正确总1次，上次正确
> **题目：Policy Gradient 推导中的期望形式转换**
> 
> 在推导 $\nabla V^{\pi_\theta}(s_0)$ 时，我们将其分解为“当前动作的梯度项”和“未来状态价值的梯度项”： 
> $$ 
> \begin{aligned} 
> \nabla V^{\pi_\theta}(s_0) &= \sum_{a_0} \pi_\theta(a_0|s_0) \left( \nabla \log \pi_\theta(a_0|s_0) \right) Q^{\pi_\theta}(s_0, a_0) \\ 
> &+ \gamma \sum_{a_0} \pi_\theta(a_0|s_0) \sum_{s_1} P(s_1|s_0, a_0) \nabla V^{\pi_\theta}(s_1) 
> \end{aligned} 
> $$ 
> 
> 请利用期望定义 $\mathbb{E}_{\tau \sim \Pr_{s_0}^{\pi_\theta}}[\cdot]$，将上述两项合并为一个关于整条轨迹 $\tau$ 的期望形式。

> [!success]- 核心思路（点击展开）
> **核心思路：概率边缘化的逆过程（积分积掉为 1）**
> 若函数 $f$ 仅依赖于轨迹的前缀（如 $s_0, a_0$），则对全轨迹 $\tau$ 的期望等价于对该前缀的期望。
> 
> **完整推导步骤：**
> 设轨迹 $\tau = (s_0, a_0, s_1, a_1, \dots)$，其概率分布为 $P(\tau|s_0) = \pi(a_0|s_0)P(s_1|s_0, a_0)\dots$
> 
> **1. 第一项（当前动作项）推导**
> $\nabla V^{\pi_{\theta}} (s_0)$拆解开的第一项只与 $a_0$ 有关，因此目标项记为 $\mathbb{E}_{\tau \sim \Pr_{s_0}^{\pi_\theta}}[g_{s_0}(a_0)]$。利用全概率公式展开 $\mathbb{E}_{\tau}$：
> $$ 
> \begin{aligned} 
> \mathbb{E}_{\tau}[g_{s_0}(a_0 )] &= \sum_{\tau} P(\tau|s_0) g_{s_0}(a_0) \\ 
> &= \sum_{a_0} \sum_{\tau_{>a_0}} \left[ \pi(a_0|s_0) P(\tau_{>a_0}|s_0, a_0) \right] g_{s_0}(a_0) \\ 
> &= \sum_{a_0} \pi(a_0|s_0) g_{s_0}(a_0) \underbrace{\left( \sum_{\tau_{>a_0}} P(\tau_{>a_0}|s_0, a_0) \right)}_{=1} \\ 
> &= \sum_{a_0} \pi(a_0|s_0) \left[ \nabla \log \pi_\theta(a_0|s_0) Q^{\pi_\theta}(s_0, a_0) \right] 
> \end{aligned} 
> $$ 
> 证得第一项等于 $\mathbb{E}_{\tau \sim \Pr_{s_0}^{\pi_\theta}} \left[ Q^{\pi_\theta}(s_0, a_0) \nabla \log \pi_\theta(a_0|s_0) \right]$。
> 
> **2. 第二项（未来价值项）推导**
> 目标项只与 $a_0, s_1$ 有关，记为 $h(s_1)$。同理展开：
> $$ 
> \begin{aligned} 
> \mathbb{E}_{\tau}[h(s_1)] &= \sum_{a_0, s_1} \sum_{\tau_{>s_1}} \left[ \pi(a_0|s_0)P(s_1|s_0, a_0) P(\tau_{>s_1}|\dots) \right] h(s_1) \\ 
> &= \sum_{a_0, s_1} \pi(a_0|s_0)P(s_1|s_0, a_0) h(s_1) \underbrace{\left( \sum_{\tau_{>s_1}} P(\tau_{>s_1}|\dots) \right)}_{=1} \\ 
> &= \sum_{a_0} \pi(a_0|s_0) \sum_{s_1} P(s_1|s_0, a_0) \nabla V^{\pi_\theta}(s_1) 
> \end{aligned} 
> $$ 
> 证得第二项等于 $\gamma \mathbb{E}_{\tau \sim \Pr_{s_0}^{\pi_\theta}} \left[ \nabla V^{\pi_\theta}(s_1) \right]$。
> 
> **3. 合并结果**
> $$ 
> \nabla V^{\pi_\theta}(s_0) = \mathbb{E}_{\tau \sim \Pr_{s_0}^{\pi_\theta}} \left[ Q^{\pi_\theta}(s_0, a_0) \nabla \log \pi_\theta(a_0|s_0) + \gamma \nabla V^{\pi_\theta}(s_1) \right] 
> $$ 

> [!info]- 错误原因（点击展开）
>第二项是全期望公式的应用，本质上就是在给定 $\left(s_{0}, a_{0}\right)$ 的条件下，对下一状态 $S_{1} \sim P\left(\cdot \mid s_{0}, a_{0}\right)$ 做一次条件期望；然后外层再对 $\left(s_{0}, a_{0}\right)$（由轨迹分布诱导）取期望。用概率论的＂塔式性质/全期望公式＂就得到右边。
> 更形式化一点（假设状态离散；连续情形把求和换成积分）：
> 令
> $$f\left(s_{1}\right)=\nabla_{\theta} V^{\pi_{\theta}}\left(s_{1}\right) .$$
> 那么对任意给定的 $\left(s_{0}, a_{0}\right)$ ，都有
> $$\sum_{s_{1}} P\left(s_{1} \mid s_{0}, a_{0}\right)f\left(s_{1}\right)=\mathbb{E}\left[f\left(S_{1}\right) \mid S_{0}=s_{0},A_{0}=a_{0}\right] .$$
> 因此
> $$\mathbb{E}_{\tau \sim P r_{s_{0}}^{\pi_{\theta}}}\left[\sum_{s_{1}} P\left(s_{1} \mid s_{0}, a_{0}\right) f\left(s_{1}\right)\right]=\mathbb{E}_{\tau \sim P r_{s_{0}}^{\pi_{\theta}}}\left[\mathbb{E}\left[f\left(S_{1}\right) \mid S_{0}, A_{0}\right]\right] .$$
> 由迭代期望（tower property）
> $$\mathbb{E}[\mathbb{E}[X \mid Y]]=\mathbb{E}[X],$$
> 取 $X=f\left(S_{1}\right) 、 Y=\left(S_{0}, A_{0}\right)$，就得到
> $$\mathbb{E}_{\tau \sim P r_{s_{0}}^{\pi_{\theta}}}\left[\mathbb{E}\left[f\left(S_{1}\right) \mid S_{0}, A_{0}\right]\right]=\mathbb{E}_{\tau \sim P r_{s_{0}}^{\pi_{\theta}}}\left[f\left(S_{1}\right)\right]=\mathbb{E}_{\tau \sim P r_{s_{0}}^{\pi_{\theta}}}\left[\nabla_{\theta} V^{\pi_{\theta}}\left(s_{1}\right)\right] .$$

---

> [!question] (2026-01-29) 第5次做，正确总1次，上次正确
> **题目：Softmax Policy Gradient Lemma 12.2 推导**
> 
> 给定 Softmax 策略：
> $$ \pi_ {\theta} (a | s) = \frac {\exp (\theta_ {s , a})}{\sum_ {a ^ {\prime}} \exp (\theta_ {s , a ^ {\prime}})} $$ 
> 已知其对数梯度为：
> $$ \frac {\partial \log \pi_ {\theta} (a | s)}{\partial \theta_ {s ^ {\prime} , a ^ {\prime}}} = \mathbb {1} [ s = s ^ {\prime} ] \left(\mathbb {1} [ a = a ^ {\prime} ] - \pi_ {\theta} (a ^ {\prime} | s)\right) $$ 
> 
> 请证明：
> $$ \frac {\partial V ^ {\pi_ {\theta}} (\mu)}{\partial \theta_ {s , a}} = \frac {1}{1 - \gamma} d _ {\mu} ^ {\pi_ {\theta}} (s) \pi_ {\theta} (a | s) A ^ {\pi_ {\theta}} (s, a) $$ 
>
> 其中 $\mu$ 为初始状态分布，$d_{\mu}^{\pi_{\theta}}(s)$ 为 初始状态为 $\mu$ 时的占用度量。
> 其它已知条件罗列如下：
> $$ \operatorname{Pr}_{\mu}^{\pi}(\tau)=\mu\left(s_{0}\right) \pi\left(a_{0} \mid s_{0}\right) P\left(s_{1} \mid s_{0}, a_{0}\right) \pi\left(a_{1} \mid s_{1}\right) \cdots .$$
> It is convenient to define the discounted total reward of a trajectory as:
> $$R(\tau):=\sum_{t=0}^{\infty} \gamma^{t} r\left(s_{t}, a_{t}\right)$$
> where $s_{t}, a_{t}$ are the state-action pairs in $\tau$. Observe that:
> $$V^{\pi_{\theta}}(\mu)=\mathbb{E}_{\tau \sim \operatorname{Pr}_{\mu}^{\pi_{\theta}}}[R(\tau)] .$$
> Policy Gradient in Advantage expression:
> $$\nabla V^{\pi_{\theta}}(\mu)=\frac{1}{1-\gamma} \mathbb{E}_{s \sim d^{\pi_{\theta}}} \mathbb{E}_{a \sim \pi_{\theta}(\cdot \mid s)}\left[A^{\pi_{\theta}}(s, a) \nabla \log \pi_{\theta}(a \mid s)\right]$$


> [!success]- 核心思路（点击展开）
> 利用策略梯度定理的优势函数形式展开：
> 
> **完整推导步骤：**
> $$ 
> \begin{aligned} 
> \frac {\partial V ^ {\pi_ {\theta}} (\mu)}{\partial \theta_ {s ^ {\prime} , a ^ {\prime}}} &= \frac {1}{1 - \gamma} \mathbb {E} _ {s \sim d _ {\mu} ^ {\pi_ {\theta}}} \mathbb {E} _ {a \sim \pi_ {\theta} (\cdot | s)} \left[ A ^ {\pi_ {\theta}} (s, a) \frac {\partial \log \pi_ {\theta} (a | s)}{\partial \theta_ {s ^ {\prime} , a ^ {\prime}}} \right] \\ 
> &= \frac {1}{1 - \gamma} \mathbb {E} _ {s \sim d _ {\mu} ^ {\pi_ {\theta}}} \mathbb {E} _ {a \sim \pi_ {\theta} (\cdot | s)} \left[ A ^ {\pi_ {\theta}} (s, a) \mathbb {1} [ s = s ^ {\prime} ] (\mathbb {1} [ a = a ^ {\prime} ] - \pi_ {\theta} (a ^ {\prime} | s)) \right] \\ 
> &= \frac {1}{1 - \gamma} d _ {\mu} ^ {\pi_ {\theta}} (s ^ {\prime}) \mathbb {E} _ {a \sim \pi_ {\theta} (\cdot | s ^ {\prime})} \left[ A ^ {\pi_ {\theta}} (s ^ {\prime}, a) (\mathbb {1} [ a = a ^ {\prime} ] - \pi_ {\theta} (a ^ {\prime} | s ^ {\prime})) \right] \\ 
> &= \frac {1}{1 - \gamma} d _ {\mu} ^ {\pi_ {\theta}} (s ^ {\prime}) \left( \mathbb {E} _ {a \sim \pi_ {\theta} (\cdot | s ^ {\prime})} [ A ^ {\pi_ {\theta}} (s ^ {\prime}, a) \mathbb {1} [ a = a ^ {\prime} ] ] - \pi_ {\theta} (a ^ {\prime} | s ^ {\prime}) \underbrace{\mathbb {E} _ {a \sim \pi_ {\theta} (\cdot | s ^ {\prime})} [ A ^ {\pi_ {\theta}} (s ^ {\prime}, a) ]}_{=0} \right) \\ 
> &= \frac {1}{1 - \gamma} d _ {\mu} ^ {\pi_ {\theta}} (s ^ {\prime}) \pi_ {\theta} (a ^ {\prime} | s ^ {\prime} ) A ^ {\pi_ {\theta}} (s ^ {\prime} , a ^ {\prime} ) 
> \end{aligned} 
> $$ 
> 其中最后一步使用了 $\sum_{a}\pi (a|s)A^{\pi}(s,a) = 0$。

> [!info]- 错误原因（点击展开）
> 这一步变换的核心在于利用指示函数（Indicator Function） $\mathbb{1}[s=s']$ 的性质，将外层的期望 $\mathbb{E}_{s \sim d_\mu^{\pi_\theta}}$ “坍缩”掉了。
> 
> **详细推导（严谨视角）：**
> 我们要处理的项是：
> $$ \mathbb{E}_{s \sim d_\mu^{\pi_\theta}} \left[ \mathbb{E}_{a \sim \pi_\theta(\cdot|s)} \left[ \dots \mathbb{1}[s=s'] \dots \right] \right] $$ 
> 
> **1. 展开外层期望**
> 根据期望的定义，$\mathbb{E}_{s \sim d_\mu^{\pi_\theta}} [f(s)] = \sum_{s \in \mathcal{S}} d_\mu^{\pi_\theta}(s) f(s)$。我们将原式展开为对状态 $s$ 的求和：
> $$ \sum_{s \in \mathcal{S}} d_\mu^{\pi_\theta}(s) \cdot \underbrace{\mathbb{E}_{a \sim \pi_\theta(\cdot|s)} \left[ A^{\pi_\theta}(s, a) \mathbb{1}[s=s'] (\mathbb{1}[a=a'] - \pi_\theta(a'|s)) \right]}_{{\text{内部项}}} $$ 
> 
> **2. 利用指示函数的“筛选”性质**
> 注意内部项里有一个 $\mathbb{1}[s=s']$。
> - 当 $s \neq s'$ 时，$\mathbb{1}[s=s'] = 0$，整个这一项直接变为 0。
> - 当且仅当 $s = s'$ 时，$\mathbb{1}[s=s'] = 1$，这一项才留存。
> 因此，这个求和式中除了 $s=s'$ 的那一项之外，其余所有项全为 0。
> 
> **3. 变量代换与坍缩**
> 既然只有 $s=s'$ 时有值，我们就直接把求和号去掉，并将式子中所有的 $s$ 替换为 $s'$：
> - $d_\mu^{\pi_\theta}(s)$ 变为 $d_\mu^{\pi_\theta}(s')$。
> - 内层期望 $\mathbb{E}_{a \sim \pi_\theta(\cdot|s)}$ 变为 $\mathbb{E}_{a \sim \pi_\theta(\cdot|s')}$。
> - $A^{\pi_\theta}(s, a)$ 变为 $A^{\pi_\theta}(s', a)$。
> - $\pi_\theta(a'|s)$ 变为 $\pi_\theta(a'|s')$。
> 
> 于是得到第三行的结果：
> $$ \frac{1}{1-\gamma} d_\mu^{\pi_\theta}(s') \mathbb{E}_{a \sim \pi_\theta(\cdot|s')} \left[ A^{\pi_\theta}(s', a) (\mathbb{1}[a=a'] - \pi_\theta(a'|s')) \right] $$ 

---


> [!question] (2026-01-29) 第3次做，正确总0次，上次错误
> **Lemma 1.16. (The performance difference lemma)**
> 
> For all policies $\pi, \pi'$ and distributions $\mu$ over $S$,
> 
> $$V^{\pi}(\mu) - V^{\pi^{\prime}}(\mu) = \frac{1}{1 - \gamma} \mathbb{E}_{s^{\prime} \sim d_{\mu}^{\pi}} \mathbb{E}_{a^{\prime} \sim \pi(\cdot \mid s^{\prime})} \left[ A^{\pi^{\prime}}(s^{\prime}, a^{\prime}) \right].$$  
> Please provide the proof.
> 其它已知条件罗列如下：
> $$ \operatorname{Pr}_{\mu}^{\pi}(\tau) = \mu(s_{0}) \pi(a_{0} \mid s_{0}) P(s_{1} \mid s_{0}, a_{0}) \pi(a_{1} \mid s_{1}) \cdots .$$
> It is convenient to define the discounted total reward of a trajectory as:
> $$R(\tau) := \sum_{t=0}^{\infty} \gamma^{t} r(s_{t}, a_{t})$$
> where $s_{t}, a_{t}$ are the state-action pairs in $\tau$. Observe that:
> $$ V^{\pi}(\mu) = \mathbb{E}_{\tau \sim \operatorname{Pr}_{\mu}^{\pi}}[R(\tau)] .$$

> [!success]- 核心思路（点击展开）
> **Proof 1: 基于轨迹的 Telescoping 展开（顺应题目已知条件）**
> 
> 通过 Telescoping 技巧，将全局的价值差转化为每一步的局部优势累加。
> 
> $$ 
> \begin{aligned} 
> V^{\pi}(\mu) - V^{\pi^{\prime}}(\mu) 
> &= \mathbb{E}_{s_0 \sim \mu} \left[ V^{\pi}(s_0) - V^{\pi^{\prime}}(s_0) \right] \\ 
> &= \mathbb{E}_{s_0 \sim \mu} \mathbb{E}_{\tau \sim \operatorname{Pr}^{\pi}(\tau \mid s_0)} \left[ \sum_{t=0}^{\infty} \gamma^t r(s_t, a_t) - V^{\pi^{\prime}}(s_0) \right] \\ 
> &\stackrel{(a)}{=} \mathbb{E}_{\tau \sim \operatorname{Pr}_{\mu}^{\pi}(\tau)} \left[ \sum_{t=0}^{\infty} \gamma^t \left( r(s_t, a_t) + \gamma V^{\pi^{\prime}}(s_{t+1}) - V^{\pi^{\prime}}(s_t) \right) \right] \\ 
> &\stackrel{(b)}{=} \mathbb{E}_{\tau \sim \operatorname{Pr}_{\mu}^{\pi}(\tau)} \left[ \sum_{t=0}^{\infty} \gamma^t \left( r(s_t, a_t) + \gamma \mathbb{E}_{s_{t+1} \sim P}[V^{\pi^{\prime}}(s_{t+1}) \mid s_t, a_t] - V^{\pi^{\prime}}(s_t) \right) \right] \\ 
> &\stackrel{(c)}{=} \mathbb{E}_{\tau \sim \operatorname{Pr}_{\mu}^{\pi}(\tau)} \left[ \sum_{t=0}^{\infty} \gamma^t \left( Q^{\pi^{\prime}}(s_t, a_t) - V^{\pi^{\prime}}(s_t) \right) \right] \\ 
> &= \mathbb{E}_{\tau \sim \operatorname{Pr}_{\mu}^{\pi}(\tau)} \left[ \sum_{t=0}^{\infty} \gamma^t A^{\pi^{\prime}}(s_t, a_t) \right] \\ 
> &= \frac{1}{1-\gamma} \mathbb{E}_{s \sim d_{\mu}^{\pi}} \mathbb{E}_{a \sim \pi(\cdot \mid s)} \left[ A^{\pi^{\prime}}(s, a) \right] 
> \end{aligned} 
> $$
> 
> 步骤 $(a)$ 是核心的 Telescoping 展开，将常数 $V^{\pi^{\prime}}(s_0)$ 拆分并吸收到级数中；步骤 $(b)$ 利用全期望公式提出状态转移的期望；步骤 $(c)$ 代入 $Q$ 函数的定义。
>
> ---
>
> **Proof 2: 基于矩阵代数的推导（更纯粹的代数解法）**
> 
> 定义状态价值向量 $V^\pi \in \mathbb{R}^{|\mathcal{S}|}$，期望奖励向量 $r^\pi \in \mathbb{R}^{|\mathcal{S}|}$，状态转移矩阵 $P^\pi \in \mathbb{R}^{|\mathcal{S}| \times |\mathcal{S}|}$。由贝尔曼方程 $V^\pi = r^\pi + \gamma P^\pi V^\pi$ 可得 $V^\pi = (I - \gamma P^\pi)^{-1} r^\pi$。
> 
> 直接计算两个策略的价值向量差：
> 
> $$
> \begin{aligned}
> V^\pi - V^{\pi'} &= (I - \gamma P^\pi)^{-1} r^\pi - V^{\pi'} \\
> &= (I - \gamma P^\pi)^{-1} \left[ r^\pi - (I - \gamma P^\pi) V^{\pi'} \right] \\
> &= (I - \gamma P^\pi)^{-1} \left[ r^\pi + \gamma P^\pi V^{\pi'} - V^{\pi'} \right]
> \end{aligned}
> $$
> 
> 括号内的项即为优势函数向量，即 $A^{\pi'}_\pi = r^\pi + \gamma P^\pi V^{\pi'} - V^{\pi'}$。
> 在等式两边左乘初始分布向量 $\mu^\top$，并代入占用测度 $(d_\mu^\pi)^\top = (1-\gamma) \mu^\top (I - \gamma P^\pi)^{-1}$：
> 
> $$
> \mu^\top (V^\pi - V^{\pi'}) = \frac{1}{1-\gamma} (d_\mu^\pi)^\top \left[ r^\pi + \gamma P^\pi V^{\pi'} - V^{\pi'} \right]
> $$
> 
> 展开为期望形式即为原引理，证毕。

> [!info]- 错误原因（点击展开）
> **背后的物理直觉：**
> 性能差引理（Performance Difference Lemma）本质上回答了“如果我把旧策略 $\pi'$ 换成新策略 $\pi$，我到底能多赚多少？”
> 
> 想象你正在执行新策略 $\pi$ 的轨迹。在每一个时刻 $t$，你面对当前状态 $s_t$，你采取了 $\pi$ 告诉你的动作 $a_t$。此时你问自己：**“就这一步而言，我采取 $a_t$，比我退回去用旧策略 $\pi'$ 多获得了多少预期收益？”** 
> 这个“单步额外收益”就是优势函数 $A^{\pi'}(s_t, a_t)$。将新策略 $\pi$ 跑出来的整条轨迹上，所有这些“局部优势”按照折扣率 $\gamma$ 累加起来，宏观上刚好就等于两个策略全局的期望总收益差。这就是强化学习中著名的“积少成多”思想，也是 TRPO 和 PPO 算法赖以生存的理论基石。

---

> [!question] (Theorem 12.4 核心推导)
> **题目：利用 Log Barrier 梯度平稳性求解 Advantage 上界**
> 
> **【背景与定义】**
> 考虑使用 Softmax 参数化的策略 $\pi_\theta$。目标函数 $L_\lambda(\theta)$ 由期望回报与 Log-Barrier 正则化项组成：
> $$L_\lambda(\theta) = V^{\pi_\theta}(\mu) + \frac{\lambda}{|\mathcal{S}||\mathcal{A}|} \sum_{s,a} \log \pi_\theta(a|s)$$
> 已知其关于参数 $\theta_{s,a}$ 的梯度表达式为：
> $$\frac{\partial L_\lambda(\theta)}{\partial \theta_{s,a}} = \underbrace{\frac{1}{1-\gamma} d_\mu^{\pi_\theta}(s) \pi_\theta(a|s) A^{\pi_\theta}(s,a)}_{\text{RL Term}} + \underbrace{\frac{\lambda}{|\mathcal{S}|}\left(\frac{1}{|\mathcal{A}|} - \pi_\theta(a|s)\right)}_{\text{Regularization Term}}$$
> 
> **【已知条件】**
> 1. **近似一阶平稳点**：$\| \nabla_\theta L_\lambda(\theta) \|_2 \leq \epsilon_{\text{opt}}$。
> 2. **正则化参数约束**：假设正则化系数足够大（或容差足够小），满足 $\epsilon_{\text{opt}} \leq \frac{\lambda}{2|\mathcal{S}||\mathcal{A}|}$。
> 3. **考虑正优势动作**：选定某个状态-动作对 $(s,a)$，满足 $A^{\pi_\theta}(s,a) > 0$（若 $A \le 0$ 则上界显然成立，无需推导）。
> 
> **【请推导】**
> 4. 证明策略下界：$\pi_\theta(a|s) \geq \frac{1}{2|\mathcal{A}|}$。
> 5. 结合上述下界与稳态分布性质 $d_\mu^{\pi_\theta}(s) \ge (1-\gamma)\mu(s)$，推导出 $A^{\pi_\theta}(s,a)$ 的最终上界 $\frac{2\lambda}{\mu(s)|\mathcal{S}|}$。

> [!success]- 核心思路（点击展开）
> **第一步：证明策略下界（Log-Barrier 的探索保证）**
> 
> 由于已知 $A^{\pi_\theta}(s,a) > 0$，且 $d_\mu^{\pi_\theta}(s)$ 和 $\pi_\theta(a|s)$ 非负，所以梯度公式中的 **RL Term 必然大于 0**。
> 利用标量绝对值受限于向量 $L_2$ 范数的性质，我们有：
> $$
> \begin{aligned}
> \epsilon_{\text{opt}} \geq \frac{\partial L_\lambda(\theta)}{\partial \theta_{s,a}} &= \text{RL Term} + \frac{\lambda}{|\mathcal{S}|}\left(\frac{1}{|\mathcal{A}|} - \pi_\theta(a|s)\right) \\
> &\geq \frac{\lambda}{|\mathcal{S}|}\left(\frac{1}{|\mathcal{A}|} - \pi_\theta(a|s)\right)
> \end{aligned}
> $$
> 将包含 $\pi_\theta(a|s)$ 的项移项并整理：
> $$\pi_\theta(a|s) \geq \frac{1}{|\mathcal{A}|} - \frac{\epsilon_{\text{opt}}|\mathcal{S}|}{\lambda}$$
> 代入已知条件 $\epsilon_{\text{opt}} \leq \frac{\lambda}{2|\mathcal{S}||\mathcal{A}|}$，可得：
> $$\pi_\theta(a|s) \geq \frac{1}{|\mathcal{A}|} - \frac{1}{2|\mathcal{A}|} = \frac{1}{2|\mathcal{A}|}$$
> *(直觉：当策略试图将某个最优动作的概率降到极低时，Log Barrier 会产生极大的正向梯度强迫其回升)*
>
> ---
>
> **第二步：反解 Advantage 上界**
> 
> 从原梯度公式中直接解出 $A^{\pi_\theta}(s,a)$：
> $$A^{\pi_\theta}(s,a) = \frac{1-\gamma}{d_\mu^{\pi_\theta}(s)} \left[ \frac{1}{\pi_\theta(a|s)} \frac{\partial L_\lambda(\theta)}{\partial \theta_{s,a}} + \frac{\lambda}{|\mathcal{S}|} \left( 1 - \frac{1}{\pi_\theta(a|s)|\mathcal{A}|} \right) \right]$$
> 将标量导数放缩为 $\epsilon_{\text{opt}}$，并利用第一步证得的 $\pi_\theta(a|s) \geq \frac{1}{2|\mathcal{A}|}$（即 $\frac{1}{\pi_\theta(a|s)} \leq 2|\mathcal{A}|$），忽略正则项括号内的负数部分（或取最宽边界）：
> $$
> \begin{aligned}
> A^{\pi_\theta}(s,a) &\leq \frac{1-\gamma}{d_\mu^{\pi_\theta}(s)} \left( 2|\mathcal{A}|\epsilon_{\text{opt}} + \frac{\lambda}{|\mathcal{S}|} \right)
> \end{aligned}
> $$
> 再次代入条件 $\epsilon_{\text{opt}} \leq \frac{\lambda}{2|\mathcal{S}||\mathcal{A}|}$（这会把括号里的第一项变成 $\frac{\lambda}{|\mathcal{S}|}$）：
> $$A^{\pi_\theta}(s,a) \leq \frac{1-\gamma}{d_\mu^{\pi_\theta}(s)} \left( \frac{\lambda}{|\mathcal{S}|} + \frac{\lambda}{|\mathcal{S}|} \right) = 2\frac{1-\gamma}{d_\mu^{\pi_\theta}(s)} \frac{\lambda}{|\mathcal{S}|}$$
> 最后，利用稳态分布的性质 $d_\mu^{\pi_\theta}(s) = (1-\gamma)\mu(s) + \gamma P(\dots) \geq (1-\gamma)\mu(s)$ 放缩分母：
> $$A^{\pi_\theta}(s,a) \leq \frac{2\lambda}{\mu(s)|\mathcal{S}|}$$
> 证明完毕。

> [!info]- 错误原因（点击展开）
> 6. **因果倒置**：不要把 $\pi \geq \frac{1}{2|\mathcal{A}|}$ 当成凭空出现的假设。它是利用 $A>0$ 截断 RL Term 后，由 Log Barrier 正则项强行撑起来的下界。
> 7. **放缩不彻底**：原版答案停在了包含 $\epsilon_{\text{opt}}$ 的中间步骤。在理论分析中，必须将 $\epsilon_{\text{opt}}$ 替换为算法设定的容差界限，并用初始分布 $\mu(s)$ 替换掉依赖于策略的 $d_\mu^{\pi_\theta}(s)$，才能得到对任意策略都成立的一致上界（Uniform Bound）。

---
