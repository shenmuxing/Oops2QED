出处：regular-papers / Tor Lattimore, *A Diffusion Analysis of Policy Gradient for Stochastic Bandits*, arXiv:2603.10219, Lemma 2 and Appendix B, PDF pp. 3 and 12；Zotero item `HU7I7KHR`，附件 `DLC6PFM7`。
标签：#policy-gradient #stochastic-bandit #softmax-parameterization #SDE #tail-bound

> [!question] (2026-06-22) 第1次做，正确总0次，上次新题
> **题目：补全 Lemma 2 的证明：为什么 softmax policy gradient 的某个参数坐标不会很快掉得特别负？**
>
> 考虑 Lattimore 的连续时间 stochastic bandit policy gradient。共有 $k$ 个 action，奖励均值满足
> $$
> 1\ge \mu_1>\mu_2\ge\cdots\ge\mu_k\ge 0,
> $$
> 策略为 softmax：
> $$
> \pi_{t,a}=\frac{\exp(\theta_{t,a})}{\sum_b\exp(\theta_{t,b})}.
> $$
> Algorithm 2 的更新为
> $$
> d\theta_t=\eta(I-\pi_t\mathbf 1^\top)dX_t,
> $$
> 其中 $dX_t=\operatorname{diag}(\pi_t)\mu\,dt+\operatorname{diag}(\sqrt{\pi_t})\Sigma^{1/2}dB_t$，且 $\|\sigma\|_\infty\le 1$。已知 Lemma 1:
> $$
> \sum_{b=1}^k \theta_{t,b}=0\qquad\text{a.s.}
> $$
> 证明 Lemma 2 的核心结论：对任意 action $a$ 和 $\delta\in(0,1)$，
> $$
> \mathbb P\!\left(\inf_{0\le t\le n}\theta_{t,a}\le -\log(n/\delta)\right)\le \delta,
> $$
> 其中常数按原文 Appendix B 的粗略化处理；若严格跟踪常数，可把阈值写成 $-\log(Cn/\delta)$。

> [!success]- 核心思路（点击展开）
> 一句话结论：**不要直接看 $\theta_{t,a}$ 的下穿；改看 $M_t=\exp(-\theta_{t,a})$ 的上穿。Lemma 1 说明只要 $\theta_{t,a}$ 很负，$\pi_{t,a}$ 就很小，于是这个坐标后续更新的漂移和噪声都被 $\pi_{t,a}$ 压住。**
>
> 固定一个 action $a$，定义
> $$
> M_t:=\exp(-\theta_{t,a}).
> $$
> 下穿事件等价于上穿事件：
> $$
> \inf_{t\le n}\theta_{t,a}\le -L
> \quad\Longleftrightarrow\quad
> \sup_{t\le n}M_t\ge e^L.
> $$
> 后面取 $L=\log(n/\delta)$，即 $e^L=n/\delta$。
>
> 关键第一步是用 Lemma 1 控制 $M_t$。因为
> $$
> \sum_{b=1}^k\theta_{t,b}=0,
> $$
> 所以至少存在某个 $b$ 使得 $\theta_{t,b}\ge 0$。于是
> $$
> M_t=\exp(-\theta_{t,a})
> \le
> \exp(\theta_{t,b}-\theta_{t,a})
> =
> \frac{\pi_{t,b}}{\pi_{t,a}}
> \le
> \frac{1}{\pi_{t,a}}.
> $$
> 这一步是 Lemma 2 的真正机制：**$\theta_{t,a}$ 越负，$\pi_{t,a}$ 越小，而 $M_t$ 最多只能按 $1/\pi_{t,a}$ 的量级放大。**
>
> 接着对 $M_t$ 用 Itô 公式。先写出第 $a$ 个坐标的漂移。令
> $$
> \bar\mu_t:=\langle\pi_t,\mu\rangle.
> $$
> 由
> $$
> d\theta_t=\eta(I-\pi_t\mathbf 1^\top)dX_t
> $$
> 可得
> $$
> d\theta_{t,a}
> =
> \eta\pi_{t,a}(\mu_a-\bar\mu_t)\,dt
> +\text{noise}.
> $$
> 在 $\|\sigma\|_\infty\le 1$ 下，二次变差满足
> $$
> d[\theta_{\cdot,a}]_t
> \le
> \eta^2\pi_{t,a}(1-\pi_{t,a})\,dt.
> $$
> 因为 $M_t=e^{-\theta_{t,a}}$，Itô 公式给出
> $$
> dM_t
> =
> -M_t\,d\theta_{t,a}
> +\frac12 M_t\,d[\theta_{\cdot,a}]_t.
> $$
> 代入上面的漂移与二次变差：
> $$
> dM_t
> \le
> M_t\left[
> \eta\pi_{t,a}(\bar\mu_t-\mu_a)
> +
> \frac{\eta^2}{2}\pi_{t,a}(1-\pi_{t,a})
> \right]dt
> +dN_t,
> $$
> 其中 $N_t$ 是局部 martingale 项。
>
> 现在用两个粗界。第一，因为 $\mu_a,\bar\mu_t\in[0,1]$，
> $$
> \bar\mu_t-\mu_a\le 1.
> $$
> 第二，由前面的 $M_t\le 1/\pi_{t,a}$，
> $$
> M_t\pi_{t,a}\le 1,
> \qquad
> M_t\pi_{t,a}(1-\pi_{t,a})\le 1.
> $$
> 所以漂移项整体被常数控制：
> $$
> dM_t
> \le
> \left(\eta+\frac{\eta^2}{2}\right)dt+dN_t.
> $$
>
> 设
> $$
> \tau_L:=\inf\{t:M_t\ge e^L\}.
> $$
> 对停止时刻 $\tau_L\wedge n$ 取期望，martingale 项期望为 $0$，得到
> $$
> \mathbb E[M_{\tau_L\wedge n}]
> \le
> M_0+\left(\eta+\frac{\eta^2}{2}\right)n.
> $$
> 又因为 $\theta_0=0$，所以 $M_0=1$。在事件 $\{\tau_L\le n\}$ 上，由连续性有
> $$
> M_{\tau_L\wedge n}=M_{\tau_L}=e^L.
> $$
> 因此
> $$
> e^L\,\mathbb P(\tau_L\le n)
> \le
> 1+\left(\eta+\frac{\eta^2}{2}\right)n.
> $$
> 即
> $$
> \mathbb P\!\left(\sup_{t\le n}M_t\ge e^L\right)
> \le
> \frac{1+(\eta+\eta^2/2)n}{e^L}.
> $$
>
> 取 $L=\log(n/\delta)$，并按文中 Appendix B 的方式把 $\eta$ 的小常数和 $n\ge3$ 的常数项粗略吸收，就得到
> $$
> \mathbb P\!\left(\sup_{t\le n}M_t\ge n/\delta\right)\le \delta.
> $$
> 换回 $\theta_{t,a}$：
> $$
> \mathbb P\!\left(\inf_{t\le n}\theta_{t,a}\le -\log(n/\delta)\right)\le \delta.
> $$
>
> **常数备注。**
> 如果完全不吸收常数，上面的推导自然给出
> $$
> \mathbb P\!\left(\inf_{t\le n}\theta_{t,a}\le -\log(Cn/\delta)\right)\le \delta
> $$
> 这种形式。原文 Lemma 2 陈述为 $\eta\le1$，但 Appendix B 证明最后一句写的是“after naively simplifying $\eta\le c$”；所以复盘时重点记机制，不要把这一步当成精细常数证明。

> [!info]- 错误原因（点击展开）
> 第一，容易误以为 Lemma 2 是因为 $\theta_{t,a}$ 有一个始终向上的正漂移。不是这样。对某个 action 来说，$\mu_a-\bar\mu_t$ 可以正也可以负，直接看 $d\theta_{t,a}$ 并不干净。
>
> 第二，真正关键是从 Lemma 1 推出
> $$
> e^{-\theta_{t,a}}\le\frac1{\pi_{t,a}}.
> $$
> 这个不等式把“坐标很负”翻译成“该 action 概率很小”，然后 $\pi_{t,a}$ 又会压住该坐标的漂移和噪声规模。
>
> 第三，事件方向容易反：$\theta$ 的下穿要换成 $M=\exp(-\theta)$ 的上穿。证明里真正控制的是
> $$
> \sup_{t\le n}M_t,
> $$
> 不是直接控制 $\inf_{t\le n}\theta_{t,a}$。
>
> 第四，这里还容易漏掉一个概率工具：这是连续时间形式的马尔科夫不等式应用。具体说，先用停止时刻
> $$
> \tau_L=\inf\{t:M_t\ge e^L\}
> $$
> 把上穿事件 $\{\sup_{t\le n}M_t\ge e^L\}$ 转成 $\{\tau_L\le n\}$，再由
> $$
> e^L\mathbf 1_{\{\tau_L\le n\}}\le M_{\tau_L\wedge n}
> $$
> 取期望得到
> $$
> \mathbb P(\tau_L\le n)\le e^{-L}\mathbb E[M_{\tau_L\wedge n}].
> $$
> 这就是离散随机变量 Markov inequality 的连续时间 stopped-process 版本。
>
> 第五，原文这里常数处理很粗。复现时如果得到 $-\log(Cn/\delta)$ 而不是刚好 $-\log(n/\delta)$，说明主线是对的；差别在最后的常数吸收。
